(func tuple (first second) '(lambda (res) (res first second)))

(func first (x y) 'x)

(func second (x y) 'y)

# Tests
(seq testtuple (tuple 1 true))
(first testtuple)   # 1
(second testtuple)  # true

(seq testtriple (tuple 1.4 (tuple 1 2)))
(first testtriple) # 1.4
(second testtriple) # (tuple 1 2)
(first (second testtriple)) # 1
(second (second testtriple)) # 2
