(func len (xs)
      (cond (isnull xs)
            0
            (plus 1 (len (tail xs)))))


(prog (xs result)
    (setq xs (cons 10 (cons 20 (cons 30 (cons 40 (cons 50 null))))))
    (setq result (len xs))
    (return result)
)