(prog (i sum)
      (setq i 0)
      (setq sum 0)
      (while (less i 10)
             (prog ()
                   (setq sum (plus sum i))
                   (setq i (plus i 1))
                   (cond (equal i 5)
                         (break))))
      (return sum)
)