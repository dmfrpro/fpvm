(func outer ()
    (prog (x)
        (
            (setq x 1)

            (func inner ()
                (setq x 2)
            )

            (inner)

            (return x)
        )
    )
)

(outer)
