(prog (i)
    (
        (setq i 0)

        (while (less i 10)
            (prog ()
                (
                    (setq i (plus i 1))
                )
            )
        )
        i
    )
)